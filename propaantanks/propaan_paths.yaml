# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: C:/datasets/propaan # dataset root dir
train: C:/datasets/propaan/train/images  # train images (relative to 'path') 128 images
val: C:/datasets/propaan/valid/images  # val images (relative to 'path') 128 images
test:  # test images (optional)

nc: 1
# Classes
names:
  0: propaan
